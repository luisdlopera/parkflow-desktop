meta {
  name: 17_active_lookup_without_locator_should_fail
  type: http
  seq: 20
}

get {
  url: {{apiBaseUrl}}/sessions/active
  body: none
  auth: none
}
headers {
  X-API-Key: {{apiKey}}
  Authorization: Bearer {{accessToken}}
}

tests {
  test("status 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
