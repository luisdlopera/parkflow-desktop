================================================================================
TOP 10 ACCIONES PRIORITARIAS - ParkFlow Backend Quality Improvement
Audit Date: 2026-07-01
Current Score: 71/100 | Target Score: 85/100
Timeline: 2 weeks (10 business days)
================================================================================

PRIORITY 1: CRITICAL (COMPLETE IMMEDIATELY)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. ADD SWAGGER @Operation DOCUMENTATION TO ALL 283 ENDPOINTS
   Scope: All 53 controllers in /modules/*/infrastructure/controller/
   Current State: 16 controllers documented (30%)
   Target: 53 controllers documented (100%)
   Effort: 2-3 days
   Impact Score: +5% (Documentation: 30% → 80%)
   
   Action Items:
   - Copy template from /modules/cash/controller/CashController.java (lines 42-50)
   - Add @Operation(summary="...", description="...") to every @GetMapping/@PostMapping/@PatchMapping
   - Add @ApiResponse for each HTTP status code (200, 201, 400, 401, 403, 404, 409, 500)
   - Regenerate OpenAPI spec: http://localhost:6011/swagger-ui.html
   
   Files to Update (Priority order):
   - /modules/configuration/controller/ConfigurationMonthlyContractController.java (0 docs)
   - /modules/parking/operation/controller/OperationController.java (0 docs)
   - /modules/settings/controller/SettingsRatesController.java (0 docs)
   - ... (34 more without documentation)
   
   Template:
   ```java
   @GetMapping
   @Operation(
       summary = "List rates",
       description = "Retrieve all active rates with optional filtering by type",
       tags = "Configuration"
   )
   @ApiResponse(responseCode = "200", description = "Rates retrieved successfully")
   @ApiResponse(responseCode = "401", description = "Unauthorized")
   public PageResponse<RateResponse> list(...) { }
   ```
   
   Success Criteria:
   ✓ All 283 endpoints have @Operation annotations
   ✓ All endpoints have @ApiResponse for success (200/201) + error codes
   ✓ Swagger UI shows complete endpoint documentation
   ✓ No warnings in IDE for missing annotations


2. STANDARDIZE PAGINATION TO PageResponse<T> (BREAKING CHANGE FOR V2.0)
   Scope: All 283 endpoints returning paginated data
   Current State: 72 PageResponse<T> + 133 Spring Page<T> + 78 no pagination
   Target: 100% PageResponse<T> (consistency)
   Effort: 3-5 days
   Impact Score: +5% (Pagination: 65% → 90%)
   
   Action Items:
   - Deprecate Spring Page<T> (mark @Deprecated in v1.1)
   - Create migration guide: docs/PAGINATION_MIGRATION.md
   - Refactor 133 endpoints returning Page<T> to PageResponse<T>
   - Update frontend client code to handle single PageResponse structure
   
   Current Inconsistency (2 different contracts):
   ❌ PageResponse<T> format:
   {
     "content": [...],
     "page": 0,
     "size": 20,
     "totalElements": 1250,
     "hasNext": true
   }
   
   ❌ Spring Page<T> format:
   {
     "content": [...],
     "pageable": { "pageNumber": 0 },
     "totalElements": 1250,
     "last": false
   }
   
   ✅ Target format (PageResponse<T> only):
   {
     "success": true,
     "data": {
       "content": [...],
       "page": 0,
       "size": 20,
       "totalElements": 1250,
       "totalPages": 63,
       "hasNext": true,
       "hasPrev": false
     },
     "meta": { ... }
   }
   
   Files to Refactor (by module):
   - configuration/ (12 endpoints)
   - cash/ (8 endpoints)
   - parking/operation/ (10 endpoints)
   - reports/ (15 endpoints)
   - licensing/ (6 endpoints)
   - ... (others)
   
   Success Criteria:
   ✓ 100% of endpoints use PageResponse<T>
   ✓ Default pagination: size=20, sort=createdAt:DESC
   ✓ Frontend handles single pagination contract
   ✓ API version bumped to v1.1 with deprecation notice


PRIORITY 2: HIGH (DO IN NEXT SPRINT)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

3. ADD SOFT DELETE (@SQLDelete) TO MUTABLE ENTITIES
   Scope: All domain entities representing business-critical data
   Current State: Only 2 entities with soft delete (Vehicle only partially)
   Target: 10+ entities with soft delete
   Effort: 1-2 days
   Impact Score: +3% (Consistency: 70% → 80%)
   
   Entities requiring soft delete (hard delete NOT allowed):
   1. Rate (2 references, delete by archival)
   2. User (account deactivation, not removal)
   3. Vehicle (hide from searches, keep history)
   4. PaymentMethod (company-specific, historical reference)
   5. MonthlyContract (contract history required)
   6. ParkingSession (audit trail must remain)
   7. CashSession (financial record, immutable after close)
   8. Company (retain for historical data)
   9. ParkingSite (legacy sites stay in DB)
   10. ConfigurationRate (pricing history)
   
   Implementation Pattern:
   ```java
   @Entity
   @SQLDelete(sql = "UPDATE rate SET deleted_at = NOW() WHERE id = ?")
   @Where(clause = "deleted_at IS NULL")
   public class Rate {
       @Column(name = "deleted_at", nullable = true)
       private OffsetDateTime deletedAt;
   }
   ```
   
   Database Change (Migration V024):
   ```sql
   ALTER TABLE rate ADD COLUMN deleted_at TIMESTAMPTZ;
   CREATE INDEX idx_rate_deleted_at ON rate(deleted_at);
   -- Similar for other 9 entities
   ```
   
   Success Criteria:
   ✓ 10+ entities annotated with @SQLDelete
   ✓ deleted_at column on all tables
   ✓ @Where(clause = "deleted_at IS NULL") on all repositories
   ✓ Soft delete queries tested


4. ADD OPTIMISTIC LOCKING (@Version) TO CRITICAL ENTITIES
   Scope: Entities subject to concurrent modification
   Current State: Only 4 @Version annotations
   Target: 8+ critical entities with versioning
   Effort: 1 day
   Impact Score: +2% (Consistency: 70% → 75%)
   
   Entities requiring @Version:
   1. Rate (frequency of updates, conflict risk)
   2. ParkingSession (entry/exit concurrent updates)
   3. CashSession (multiple operators modifying)
   4. MonthlyContract (status changes)
   5. PaymentMethod (configuration changes)
   6. Company (settings updates)
   7. ParkingSpace (availability updates)
   8. Vehicle (plate/status changes)
   
   Implementation:
   ```java
   @Entity
   public class Rate {
       @Version
       private Long version;
       
       // ... other fields
   }
   ```
   
   Database Change (Migration):
   ALTER TABLE rate ADD COLUMN version BIGINT DEFAULT 0;
   -- Similar for other 7 entities
   
   Error Handling (already present in GlobalExceptionHandler):
   OptimisticLockException → HTTP 409 Conflict
   Message: "La operacion no pudo completarse porque otro usuario..."
   
   Success Criteria:
   ✓ 8+ entities have @Version annotation
   ✓ version column on all tables (default 0)
   ✓ Concurrent update test passes (409 Conflict on retry)
   ✓ Error message guides user to retry


5. CREATE BASEENTITY FOR AUDIT FIELD CONSOLIDATION
   Scope: All domain entities
   Current State: 46 @PrePersist/@PreUpdate scattered, no inheritance
   Target: Single BaseEntity with unified audit logic
   Effort: 0.5-1 day
   Impact Score: +1% (Consistency: 70% → 72%)
   
   Implementation (new file):
   File: /modules/common/domain/BaseEntity.java
   
   ```java
   @MappedSuperclass
   public abstract class BaseEntity {
       @Column(name = "created_at", nullable = false, updatable = false)
       protected OffsetDateTime createdAt;
       
       @Column(name = "updated_at", nullable = false)
       protected OffsetDateTime updatedAt;
       
       @PrePersist
       protected void onCreate() {
           this.createdAt = OffsetDateTime.now(ZoneId.of("UTC"));
           this.updatedAt = this.createdAt;
       }
       
       @PreUpdate
       protected void onUpdate() {
           this.updatedAt = OffsetDateTime.now(ZoneId.of("UTC"));
       }
   }
   ```
   
   Migration Path:
   - Create BaseEntity (new file)
   - Make 50+ entities extend BaseEntity
   - Remove 46 @PrePersist/@PreUpdate duplicates
   - Verify all timestamps consistent
   
   Entities to Update:
   - All in /domain/ folders across 14 modules
   - Example: Rate, ParkingSession, CashSession, etc.
   
   Success Criteria:
   ✓ BaseEntity created with @MappedSuperclass
   ✓ 50+ entities extend BaseEntity
   ✓ No duplicate @PrePersist/@PreUpdate code
   ✓ All timestamps use UTC timezone
   ✓ Tests verify audit fields auto-populated


PRIORITY 3: MEDIUM (DO IN NEXT 2 SPRINTS)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

6. ADD @EntityGraph TO HIGH-N+1 REPOSITORIES
   Scope: Repositories with nested entity loads
   Current State: Zero @EntityGraph annotations
   Target: 5+ repositories with eager loading strategies
   Effort: 1-2 days
   Impact Score: +2% (Performance: 65% → 72%)
   
   High N+1 Risk Repositories:
   1. CashSessionRepository.findById() → loads CashMovements
   2. ParkingSessionRepository.findById() → loads EntryExit, Payments
   3. SubscriptionRepository.findByCompanyId() → loads Plan, Company
   4. RateRepository.findAll() → loads RateFractions
   5. MonthlyContractRepository.findByPlate() → loads Vehicle, Company
   
   Implementation:
   ```java
   @Repository
   public interface CashSessionRepository extends JpaRepository<CashSession, UUID> {
       @EntityGraph(attributePaths = {"movements", "operator"})
       CashSession findById(UUID id);
       
       @EntityGraph(attributePaths = "movements")
       List<CashSession> findByCompanyIdAndStatusOrderByCreatedAtDesc(UUID companyId, String status);
   }
   ```
   
   Alternative: Projection for read-heavy endpoints:
   ```java
   interface CashSessionSummary {
     UUID getId();
     String getStatus();
     BigDecimal getTotalAmount();
   }
   
   @Query("SELECT new com.parkflow...CashSessionSummary(s.id, s.status, s.totalAmount) FROM CashSession s")
   List<CashSessionSummary> listSessions();
   ```
   
   Success Criteria:
   ✓ 5+ repositories use @EntityGraph
   ✓ Query count reduced by 50% on high-traffic endpoints
   ✓ Performance test shows <2s latency for 1000 records
   ✓ No N+1 warnings in monitoring


7. MIGRATE ALL @PreAuthorize(hasAnyRole...) TO hasAuthority(...)
   Scope: All 241 @PreAuthorize annotations
   Current State: Mixed strategy (authority + role, both allowed)
   Target: Authority-only (fine-grained permissions)
   Effort: 1-2 days
   Impact Score: +1% (Security: 80% → 82%)
   
   Current Mixed Strategy (problematic):
   @PreAuthorize("hasAnyRole('SUPER_ADMIN','ADMIN','OPERADOR')")  ❌ Coarse
   @PreAuthorize("hasAuthority('cierres_caja:abrir')")            ✅ Fine-grained
   
   Target (authority-only):
   @PreAuthorize("hasAuthority('cierres_caja:abrir') OR hasAuthority('reportes:leer')")
   
   Action Items:
   - Search & replace: hasAnyRole → hasAuthority
   - Map old roles to authorities:
     SUPER_ADMIN → auth:super_admin
     ADMIN → auth:admin
     OPERADOR → operador:general
   - Create PermissionRegistry documenting all 200+ authorities
   - Test with 5+ different user roles
   
   Files Affected:
   - 53 controllers with @PreAuthorize annotations
   - Every endpoint's permission requirement mapped
   
   Success Criteria:
   ✓ 0 hasAnyRole() annotations remaining
   ✓ 100% authority-based (hasAuthority)
   ✓ PermissionRegistry documents all 200+ authorities
   ✓ All tests pass with fine-grained permissions


8. CREATE API ERROR CODES REFERENCE DOCUMENT
   Scope: All error scenarios across 283 endpoints
   Current State: 20+ exception types mapped, but not documented
   Target: Complete error codes reference (docs/API_ERROR_CODES.md)
   Effort: 0.5-1 day
   Impact Score: +2% (Documentation: 30% → 35%)
   
   Document Structure:
   File: docs/API_ERROR_CODES.md
   
   Format:
   # Error Code Reference
   
   ## HTTP 400 Bad Request
   
   | Error Code | Message | Cause | Solution |
   |------------|---------|-------|----------|
   | VALIDATION_ERROR | Revisa los datos ingresados | Missing/invalid fields | Check ValidationIssue array |
   | INVALID_ARGUMENT | La solicitud no es valida | Wrong argument type | Review endpoint spec |
   | MALFORMED_REQUEST | JSON no pudo leerse | Invalid JSON syntax | Fix JSON formatting |
   | TYPE_MISMATCH | El parametro debe ser {tipo} | Query param type wrong | Use correct type |
   
   ## HTTP 401 Unauthorized
   
   | Error Code | Message | Cause | Solution |
   |------------|---------|-------|----------|
   | AUTH_UNAUTHORIZED | Tu sesion ha expirado | Token expired | Re-login |
   | INVALID_CREDENTIALS | Usuario/password incorrecto | Wrong login | Check credentials |
   
   ... (continue for all 200+ error codes)
   
   Source: GlobalExceptionHandler.java (518 lines)
   
   Success Criteria:
   ✓ Docs/API_ERROR_CODES.md created
   ✓ All 200+ error codes documented with examples
   ✓ HTTP status codes mapped to error codes
   ✓ Solutions provided for each error


9. ADD SOFT DELETE TO REPORTQUERYSERVICE MIGRATION GUIDE
   Scope: Deprecated ReportQueryService (361 lines, 11 public methods)
   Current State: Marked @Deprecated but no migration path
   Target: Replacement services available + migration guide
   Effort: 2-3 days
   Impact Score: +1% (Evolution: 60% → 65%)
   
   ReportQueryService Methods (need replacements):
   1. dailyOperations() → DailyReportsQueryService
   2. dailyRevenue() → CashReportsQueryService
   3. dailyMovementsByType() → CashReportsQueryService
   4. dailyPaymentMethods() → PaymentReportService
   5. vehicleCapacity() → CapacityReportService
   6. Lost Ticket Report → IncidentReportService
   7. Monthly Summary → MonthlyReportService
   8. Vehicle Type Stats → VehicleTypeReportService
   9. Site Performance → SiteReportService
   10. Duration Analysis → DurationAnalysisService
   11. Occupancy Forecast → PredictionService
   
   Replacement Structure:
   - Create 3-4 new report services
   - Each focused on single domain (Daily, Cash, Vehicles, Capacity)
   - Remove ReportQueryService in v2.0
   
   Migration Guide (docs/REPORT_MIGRATION.md):
   Old API → New API mapping
   Error handling for legacy clients
   Sunset timeline
   
   Success Criteria:
   ✓ ReportQueryService marked @Deprecated(forRemoval=true)
   ✓ 3-4 replacement services created
   ✓ docs/REPORT_MIGRATION.md published
   ✓ All endpoints have migration path
   ✓ Sunset date: 2026-12-31


10. CREATE FEATURE FLAGS / API VERSIONING STRATEGY
    Scope: Multi-version API support (v1 → v2 → v3)
    Current State: Single v1 only
    Target: v1/v2 with feature flags for gradual rollout
    Effort: 2-3 days
    Impact Score: +1% (Evolution: 60% → 68%)
    
    Strategy (Semantic Versioning):
    - v1: Current (stability, no breaking changes)
    - v1.1: Deprecations added (v2.0 prep)
    - v2.0: Breaking changes applied (PageResponse<T>, etc.)
    - v3.0: (Future, planned changes)
    
    Feature Flag Implementation:
    ```java
    @Service
    public class FeatureFlagService {
        public boolean isEnabled(String featureName, UUID companyId) {
            // Check tenant override, then global
            return getFeatureFlagValue(featureName, companyId);
        }
    }
    ```
    
    Flags for v2.0 Rollout:
    - pagination_v2_enabled
    - error_codes_v2_format
    - soft_delete_enabled
    - optimistic_lock_enabled
    
    Documentation (docs/VERSIONING_STRATEGY.md):
    - Breaking change policy
    - Feature rollout timeline
    - Client migration paths
    - Sunset schedule
    
    Success Criteria:
    ✓ Feature flag infrastructure implemented
    ✓ 4+ flags for v2.0 features
    ✓ docs/VERSIONING_STRATEGY.md published
    ✓ Gradual rollout plan created
    ✓ Tenant-specific overrides working


================================================================================
IMPLEMENTATION ROADMAP
================================================================================

Week 1 (Sprint 1) — Documentation + Pagination
├── Day 1-2: Add @Operation to all 283 endpoints (Actions #1)
├── Day 3-5: Standardize pagination to PageResponse<T> (Action #2)
└── Result: Score 71 → 76 (+5%)

Week 2 (Sprint 2) — Data Consistency + Performance
├── Day 1: Add @Version to 8 entities (Action #4)
├── Day 1.5: Create BaseEntity (Action #5)
├── Day 2: Add @EntityGraph to 5 repositories (Action #6)
├── Day 2.5: Add soft delete to 10 entities (Action #3)
└── Result: Score 76 → 85 (+9%)

Post-85% (Nice-to-have)
├── Migrate @PreAuthorize to authority-only (Action #7)
├── Create API error codes reference (Action #8)
├── ReportQueryService migration guide (Action #9)
└── Feature flags implementation (Action #10)

================================================================================
EXECUTION GUIDELINES
================================================================================

1. Code Review Checklist
   ✓ All new @Operation have summary + description
   ✓ All endpoints have @ApiResponse for 200, 4xx, 5xx codes
   ✓ All PageResponse<T> use consistent structure
   ✓ No Spring Page<T> remaining
   ✓ All entities with @Version pass concurrency tests
   ✓ All soft-deleted entities have deleted_at column

2. Testing Requirements
   ✓ Integration tests for Swagger documentation
   ✓ Pagination compatibility test (frontend)
   ✓ Optimistic lock conflict test (manual update)
   ✓ Soft delete query test (deleted_at IS NULL)
   ✓ N+1 query test (query count monitoring)

3. Documentation
   ✓ Update CLAUDE.md with new patterns
   ✓ Create docs/MIGRATION_GUIDE.md for v1.1 → v2.0
   ✓ Publish API_ERROR_CODES.md
   ✓ Update CHANGELOG.md per action

4. Stakeholder Communication
   ✓ Notify frontend team of pagination changes
   ✓ Announce v2.0 migration timeline (2026-09-01)
   ✓ Share API_ERROR_CODES.md with mobile team
   ✓ Publish Swagger UI changes in release notes

================================================================================
Questions? Refer to backend-audit-2026-07-01/reporte-completo.md for full analysis
