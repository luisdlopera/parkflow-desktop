{
  "name": "@parkflow/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "test:unit": "cargo test --manifest-path src-tauri/Cargo.toml --lib",
    "test:integration": "cargo test --manifest-path src-tauri/Cargo.toml --test licensing_persistence",
    "test:e2e": "pnpm exec playwright test --config playwright.config.ts",
    "test": "pnpm test:unit && pnpm test:integration"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.6"
  }
}
