{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "licensing",
  "description": "Licensing uses Rust-side storage (dirs/keyring); same core IPC as default",
  "windows": ["main"],
  "permissions": ["core:default"]
}
